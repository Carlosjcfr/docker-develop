# GitLab Configuration
GITLAB_VERSION=latest

# Ports
GITLAB_HTTP_PORT=8929
GITLAB_SSH_PORT=8922

# Arcane Metadata
ARCANE_ICON="si:gitlab"
ARCANE_CATEGORY="DevOps"

# Secrets (will be auto-generated if not present)
# GITLAB_ROOT_PASSWORD is handled by the script
